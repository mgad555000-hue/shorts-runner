# دليل بناء وصفات MG Ranner

---

## ما هو النظام؟

**MG Ranner** هو نظام لتشغيل وصفات أوتوماتيكية لنوعين من المحتوى: **شورتس (shorts)** و **لونج (long)**. الوصفة = ملف JSON فيه خطوات بتتنفذ بالترتيب.

**كل خطوة** بتعمل حاجة واحدة: تقرأ ملف، تبعت لـ AI، تنسّق نص، تحوّل لصوت... إلخ.

---

## بنية المجلدات

```
shorts-runner/
├── recipes/                          ← مكان ملفات الوصفات (JSON)
│   └── اسم_الوصفة.json
│
├── shorts/out/                       ← مخرجات وصفات الشورتس
├── longs/out/                        ← مخرجات وصفات اللونج
│
└── data/channels/
    └── اسم_القناة/
        └── اسم_الوصفة/              ← نفس اسم ملف الـ JSON بدون الامتداد
            ├── input/                ← الملفات المطلوبة (يحطها المستخدم)
            └── output/              ← النتائج (بتتنسخ هنا تلقائياً)
```

### مثال:
- وصفة اسمها `script_generator` لقناة `My_Kidney`:
  - ملف الوصفة: `recipes/script_generator.json`
  - ملفات المستخدم: `data/channels/My_Kidney/script_generator/input/`
  - النتائج: `data/channels/My_Kidney/script_generator/output/`

---

## كيف تعمل الوصفة

### أنواع الوصفات:
كل وصفة لها **نوع محتوى** يتحدد عند تسجيلها في الواجهة:
- **shorts** — وصفة للفيديوهات القصيرة (المخرجات تروح `shorts/out/`)
- **long** — وصفة للفيديوهات الطويلة (المخرجات تروح `longs/out/`)
- **both** — وصفة تظهر في الاتنين

**ملاحظة:** النوع بيأثر على الفلترة في الواجهة ومسار الإخراج فقط. ملف الوصفة JSON نفسه مفيهوش نوع — النوع بيتختار من الواجهة عند التسجيل.

### هيكل ملف الوصفة:
```json
{
  "name": "اسم الوصفة",
  "steps": [
    {"id": "خطوة1", "action": "نوع_الأمر", ...},
    {"id": "خطوة2", "action": "نوع_الأمر", ...}
  ]
}
```

### قواعد أساسية:
1. كل خطوة لها `id` فريد (اسم مميز غير مكرر)
2. كل خطوة لها `action` (نوع الأمر المطلوب تنفيذه)
3. أي خطوة تقدر تستخدم نتيجة خطوة **سابقة** بكتابة `{id_الخطوة_السابقة}`
4. الخطوة الأخيرة اللي فيها `save_as` هي اللي بتحفظ النتيجة النهائية
5. **تنسيق MG Ranner (إلزامي):** كل نصوص المدخلات والمخرجات لازم تكون بتنسيق MG Ranner — كل موضوع محدد بـ `<<<SCRIPT_N>>>...<<<END_SCRIPT>>>` (أو `<<<INTRO_N>>>` للمقدمات). في السكريبتات الطويلة (لونج)، كل جزء داخل الموضوع محدد بـ `<<<PART_N>>>...<<<END_PART>>>`.

---

## الأوامر المتاحة (18 أكشن)

---

### 1. `read_input` — قراءة ملف نصي
يقرأ ملف من مجلد `input` ويخزن محتواه.

```json
{
  "id": "اسم_فريد",
  "action": "read_input",
  "file": "اسم_الملف.txt"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `file` | نعم | اسم الملف في مجلد input |

---

### 2. `read_json` — قراءة ملف JSON
يقرأ ملف JSON من مجلد `input` ويخزن محتواه ككائن بيانات.

```json
{
  "id": "اسم_فريد",
  "action": "read_json",
  "file": "اسم_الملف.json"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `file` | نعم | اسم الملف في مجلد input |

---

### 3. `template` — تجميع نصوص
يدمج نتائج خطوات سابقة في نص واحد.

```json
{
  "id": "اسم_فريد",
  "action": "template",
  "text": "التعليمات:\n{خطوة1}\n\nالبيانات:\n{خطوة2}",
  "save_as": "اسم_الملف.txt"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `text` | نعم | النص مع references لخطوات سابقة `{step_id}` |
| `save_as` | لا | اسم ملف للحفظ في output |

---

### 4. `generate` — توليد نص بالذكاء الاصطناعي
يبعت برومبت لنموذج AI (مثل Gemini) ويستقبل النص المولّد.

```json
{
  "id": "اسم_فريد",
  "action": "generate",
  "input": "{خطوة_البرومبت}",
  "system_prompt": "تعليمات عامة للـ AI",
  "temperature": 0.7,
  "max_tokens": 8000,
  "save_as": "اسم_الملف.txt"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | البرومبت (نص مباشر أو `{خطوة_سابقة}`) |
| `system_prompt` | لا | تعليمات عامة للـ AI (الدور، الأسلوب) |
| `temperature` | لا | الإبداعية (0.0 = دقيق، 1.0 = إبداعي). الافتراضي: 0.7 |
| `max_tokens` | لا | الحد الأقصى للنص المولّد |
| `save_as` | لا | اسم ملف للحفظ في output |

**ملاحظة مهمة:** `input` هو البرومبت الرئيسي (اللي فيه البيانات + التعليمات التفصيلية). `system_prompt` هو تعليمات عامة قصيرة اختيارية.

---

### 5. `format_text` — تنسيق النص بواسطة بايثون
يعالج النص بعد استلامه من الـ AI. بيعمل:
- إضافة مسافات بعد علامات الترقيم (فاصلة ، ونقطة .)
- تلوين كلمات محددة بوسم `<r>كلمة</r>`

**مهم: التنسيق ده بيعمله بايثون بدقة — مش الـ AI. فلا تطلب من الـ AI في تعليماته يعمل مسافات أو تلوين.**

```json
{
  "id": "اسم_فريد",
  "action": "format_text",
  "input": "{خطوة_النص}",
  "color_words": ["بس", "ده", "دي", "دى", "دول"],
  "spaces_after_punctuation": 5,
  "save_as": "اسم_الملف.txt"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | النص المراد تنسيقه `{خطوة_سابقة}` |
| `color_words` | لا | قائمة الكلمات اللي هتتلون. الافتراضي: `["بس", "ده", "دي", "دى", "دول"]` |
| `spaces_after_punctuation` | لا | عدد المسافات بعد الفاصلة والنقطة. الافتراضي: 5 |
| `save_as` | لا | اسم ملف للحفظ في output |

**الترتيب: `format_text` دايماً بتيجي بعد `generate` مباشرة.**

---

### 6. `tts` — تحويل نص لصوت
يحوّل النص لملف صوتي WAV + MP3.

```json
{
  "id": "اسم_فريد",
  "action": "tts",
  "input": "{خطوة_النص}",
  "max_chars": 5000,
  "save_as": "audio"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | النص المراد تحويله `{خطوة_سابقة}` |
| `max_chars` | لا | الحد الأقصى لعدد الحروف |
| `save_as` | لا | اسم الملف **بدون امتداد** — النظام بيضيف `.wav` و `.mp3` تلقائي |

**اختيار المزود** (متغيرات بيئة، مش من الوصفة): `TTS_PROVIDER` = `vertex` (الافتراضي) / `elevenlabs` / `minimax` / `grok` + `TTS_VOICE_ID`. مزود **grok** (xAI، ~4.20 دولار/مليون حرف): أصواته `ara/eve/rex/sal/leo` واللهجة من `GROK_TTS_LANGUAGE` (الافتراضي `ar-EG` المصرية)، ومفتاحه `XAI_API_KEY`.

---

### 7. `transcribe` — تحويل صوت لنص
يحوّل ملف صوتي لنص مكتوب (باستخدام Whisper).

```json
{
  "id": "اسم_فريد",
  "action": "transcribe",
  "input": "{خطوة_الصوت}",
  "language": "ar",
  "save_as": "transcript.txt"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | ملف الصوت `{خطوة_سابقة}` |
| `language` | لا | لغة الصوت (مثلاً `"ar"` للعربي) |
| `save_as` | لا | اسم ملف للحفظ في output |

---

### 8. `batch_send` — إرسال دفعة برومبتات
يبعت قائمة برومبتات مرة واحدة لـ AI (أرخص وأسرع للكميات الكبيرة).

```json
{
  "id": "اسم_فريد",
  "action": "batch_send",
  "prompts": "{خطوة_القائمة}",
  "system_prompt": "تعليمات للـ AI",
  "temperature": 0.7,
  "max_tokens": 8192,
  "save_as": "batch_job_info.json"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `prompts` | نعم | قائمة البرومبتات `{خطوة_سابقة}` |
| `system_prompt` | لا | تعليمات عامة للـ AI |
| `temperature` | لا | الإبداعية. الافتراضي: 0.7 |
| `max_tokens` | لا | الحد الأقصى لكل رد. الافتراضي: 8192 |
| `save_as` | لا | ملف لحفظ معلومات الدفعة |

---

### 9. `batch_retrieve` — استقبال نتائج الدفعة
يستنى ويستقبل نتائج دفعة تم إرسالها.

```json
{
  "id": "اسم_فريد",
  "action": "batch_retrieve",
  "input": "{خطوة_الإرسال}",
  "poll_interval": 30,
  "max_wait": 3600
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | خطوة الـ batch_send السابقة |
| `poll_interval` | لا | الفترة بين كل محاولة استقبال (بالثواني). الافتراضي: 30 |
| `max_wait` | لا | الحد الأقصى للانتظار (بالثواني). الافتراضي: 3600 (ساعة) |

---

### 10. `save_file` — حفظ محتوى في ملف
يحفظ أي محتوى (نص أو JSON) في ملف في output.

```json
{
  "id": "اسم_فريد",
  "action": "save_file",
  "input": "{خطوة_سابقة}",
  "save_as": "اسم_الملف.txt"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | المحتوى المراد حفظه |
| `save_as` | نعم | اسم الملف في output |

---

### 11. `save_docx` — حفظ كملف Word
يحفظ النص المنسق كملف Word مع تلوين الكلمات المحاطة بـ `<r>` بالأحمر.

```json
{
  "id": "اسم_فريد",
  "action": "save_docx",
  "input": "{خطوة_النص_المنسق}",
  "save_as": "output.docx",
  "font_size": 14,
  "marker_prefix": "SCRIPT"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | النص المنسق `{خطوة_سابقة}` |
| `save_as` | نعم | اسم ملف Word (لازم ينتهي بـ `.docx`) |
| `font_size` | لا | حجم الخط. الافتراضي: 14 |
| `font_name` | لا | نوع الخط. الافتراضي: Arial |
| `marker_prefix` | لا | بادئة الماركر (SCRIPT للسكريبتات، INTRO للمقدمات). الافتراضي: SCRIPT |
| `line_spacing` | لا | المسافة بين السطور. الافتراضي: 28 |

---

### 12. `read_docx` — قراءة ملف/ملفات Word
يقرأ ملف Word واحد أو كل ملفات Word الموجودة في مجلد input.

```json
{
  "id": "اسم_فريد",
  "action": "read_docx",
  "file": "scripts_output.docx",
  "reconstruct_markers": true,
  "marker_prefix": "SCRIPT"
}
```

**لقراءة كل ملفات Word في input (بدون تحديد اسم ملف):**
```json
{
  "id": "اسم_فريد",
  "action": "read_docx",
  "reconstruct_markers": true
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `file` | لا | اسم ملف محدد. لو مش موجود → يقرأ كل ملفات `.docx` في input |
| `reconstruct_markers` | لا | `true` = يكشف headings "Script N" ويعيد بناء الماركرز `<<<SCRIPT_N>>>` و `<<<END_SCRIPT>>>` بدقة. الافتراضي: `false` |
| `marker_prefix` | لا | بادئة الماركر (SCRIPT أو INTRO). الافتراضي: SCRIPT |

**مهم:** استخدم `reconstruct_markers: true` لما تقرأ Word محفوظ بـ `save_docx` — عشان بايثون يعيد بناء الماركرز بدقة 100% بدل ما تعتمد على الـ AI يفهمها.

---

### 13. `read_excel` — قراءة ملف Excel
يقرأ ملف Excel من مجلد input ويرجع القائمة كنص منسق.

```json
{
  "id": "اسم_فريد",
  "action": "read_excel",
  "file": "videos_list.xlsx"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `file` | نعم | اسم ملف Excel في مجلد input |

**الخرج:** نص بتنسيق `(رقم) عنوان` — سطر لكل صف في الإكسيل.

---

### 14. `copy_videos` — نسخ فيديوهات حسب مخرجات AI
يحلل مخرجات AI ويستخرج أرقام الفيديوهات من كل سكريبت وينسخها في مجلدات منفصلة.

```json
{
  "id": "اسم_فريد",
  "action": "copy_videos",
  "input": "{خطوة_مخرجات_AI}",
  "marker_prefix": "SCRIPT"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | مخرجات AI اللي فيها أرقام الفيديوهات `{خطوة_سابقة}` |
| `marker_prefix` | لا | بادئة الماركر. الافتراضي: SCRIPT |
| `videos_dir` | لا | مسار الفيديوهات. الافتراضي: `/app/data/channels/{channel}/videos` |

**كيف يشتغل:**
1. يقسم النص بالماركرز `<<<SCRIPT_N>>>`
2. يستخرج أرقام الفيديوهات من كل قسم (الأرقام اللي بين أقواس قبل `(مطابق)` أو `(تقريبي)`)
3. ينشئ مجلد `SCRIPT_N/` في output لكل سكريبت
4. ينسخ الفيديوهات من مجلد القناة للمجلد المخصص

**تنسيق المخرجات المتوقع من AI:**
```
<<<SCRIPT_12>>>
1) عنوان الفيديو (45) (مطابق).
كلمات القسم الأول من السكريبت...

2) عنوان الفيديو (7) (تقريبي).
كلمات القسم الثاني...
<<<END_SCRIPT>>>
```

---

### 15. `tts_multi` — تحويل نصوص بتنسيق MG Ranner لملفات صوتية
يقرأ نص بماركرز ويحوّل كل سكريبت (أو كل جزء) لملف صوتي WAV + MP3.
**يدعم الحالتين تلقائياً:**
- شورتس (بدون PART): كل سكريبت → ملف واحد
- لونج (مع PART): كل جزء → ملف منفصل

```json
{
  "id": "اسم_فريد",
  "action": "tts_multi",
  "input": "{خطوة_النص}",
  "marker_prefix": "SCRIPT",
  "max_chars": 5000
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | النص بتنسيق MG Ranner `{خطوة_سابقة}` |
| `marker_prefix` | لا | بادئة الماركر. الافتراضي: SCRIPT |
| `max_chars` | لا | الحد الأقصى لعدد الحروف لكل قطعة |

**المخرجات:**
- شورتس: `SCRIPT_101.wav` + `SCRIPT_101.mp3`, `SCRIPT_102.wav` + `SCRIPT_102.mp3`...
- لونج: `SCRIPT_101_1.wav` + `SCRIPT_101_1.mp3`, `SCRIPT_101_2.wav` + `SCRIPT_101_2.mp3`...

---

### 16. `review_build_cards` — بناء كروت مراجعة توافق النصوص مع المقدمات
يقرأ ملفات المراجعة الثلاثة من `input/` (مقدمات + نصوص + عناوين) ويبني «كارت» لكل موضوع (عنوان + 4 مقدمات + 4 نصوص) مع **بوابة اكتمال بنيوية**: عدد المقدمات/النصوص، أجزاء ناقصة أو فارغة، عنوان مفقود، محتوى قصير بشكل غير طبيعي، ترميز فاسد، بقايا تنسيق، وتكرار حرفي لنفس المحتوى في أكثر من مكان. التقرير البنيوي يتحفظ دايماً في `structure_report.txt`.

```json
{
  "id": "cards",
  "action": "review_build_cards",
  "intros_file": "intros_output.docx",
  "texts_file": "texts_output.docx",
  "topics_file": "topics.json",
  "expected_pairs": 4,
  "strict": true
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `intros_file` | لا | ملف المقدمات (فواصل `Script N`). الافتراضي: `intros_output.docx` |
| `texts_file` | لا | ملف النصوص (`Script N` + `Part K`). الافتراضي: `texts_output.docx` |
| `topics_file` | لا | ملف العناوين `{"titles": [{"id", "title"}]}`. الافتراضي: `topics.json` |
| `expected_pairs` | لا | عدد المقدمات/النصوص لكل موضوع. الافتراضي: 4 |
| `strict` | لا | `true` = أي مشكلة بنيوية توقف الوصفة قبل أي صرف API. الافتراضي: `true` |
| `min_words` | لا | أقل عدد كلمات طبيعي للمقدمة/النص. الافتراضي: 30 |
| `intro_end_marker` | لا | ماركر نهاية المقدمة (متسامح مع التشكيل). الافتراضي: `تفاصيل أكتر في الفيديو التوضيحي التالي` |

**بيحترم TOPIC_IDS** المختارة من الواجهة.

---

### 17. `review_build_prompts` — برومبت مستقل لكل موضوع
يحول الكروت لقائمة برومبتات جاهزة لـ `batch_send`: كل برومبت = تعليمات الحكم + كارت موضوع واحد + صيغة إخراج JSON إلزامية بترجيع `topic_id` (عشان ربط النتائج ما يعتمدش على ترتيب الباتش).

```json
{
  "id": "prompts",
  "action": "review_build_prompts",
  "input": "{cards}",
  "instructions": "{instructions}"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | نتيجة `review_build_cards` |
| `instructions` | لا | تعليمات الحكم (من `read_input`) |

---

### 18. `review_parse_verdicts` — تحليل أحكام الباتش وبناء التقرير
يربط كل رد بموضوعه عبر `topic_id` المرجَّع جوه الرد نفسه، ويتحقق من كل زوج (النص مع مقدمته + المقدمة مع العنوان + النص مع العنوان). أي رد تالف أو حكم بقيمة غير معتمدة = «غير محكوم» صريح (مفيش فقد صامت). التفاصيل الكاملة في JSON والملخص النصي بيرجع من الأكشن، والخلاصة النهائية سطر واحد صريح: `✅ كل الموضوعات مطابقة` أو `⚠️ غير مطابق: الموضوع N نص K...`.

```json
{
  "id": "report",
  "action": "review_parse_verdicts",
  "input": "{verdicts_raw}",
  "cards": "{cards}",
  "save_json": "review_report.json"
}
```

| المعامل | مطلوب؟ | الوصف |
|---------|--------|-------|
| `input` | نعم | نتيجة `batch_retrieve` (قائمة الردود) |
| `cards` | نعم | نتيجة `review_build_cards` |
| `save_json` | لا | ملف التفاصيل. الافتراضي: `review_report.json` |

**ضمانات السلامة في التحليل:** ردّان بنفس `topic_id` = الموضوع «غير محكوم» (مفيش «آخر واحد يكسب»)؛ زوج مكرر في نفس الرد = «غير محكوم»؛ رد بـ topic_id مش من المواضيع المرسلة = «رد شاذ» بيمنع «✅»؛ قيمة حكم غير «مطابق»/«غير مطابق» (بعد تطبيع التشكيل والمسافات) = «غير محكوم». السطر «✅ كل الموضوعات مطابقة» مايطلعش إلا بصفر غير مطابق + صفر غير محكوم + صفر مشاكل بنيوية + صفر ردود شاذة.

**ملاحظات تشغيلية لوصفة المراجعة:**
- الموديل ومستوى التفكير بيتحددوا من الواجهة وقت التشغيل — مفيش موديل مثبت في الوصفة. الموديل لازم يدعم الباتش (Gemini/Vertex/Claude/GLM) — موديل بلا باتش (زي grok) بيرفض برسالة واضحة قبل أي صرف.
- أي وضع تشغيل من الواجهة (instant/send_only/receive_only/batch_auto) بينفذ الوصفة مباشرة لأن الباتش صريح جوه خطواتها.
- **حد معروف:** لو الرن اتقطع بعد الإرسال وقبل الاستقبال، إعادة التشغيل بتبعت باتش جديد (بصرف جديد) — معلومات الباتش القديم محفوظة في `batch_job_info.json` في output للرجوع اليدوي.

---

## مثال كامل: وصفة توليد سكريبتات

### ملف الوصفة: `recipes/script_generator.json`
```json
{
  "name": "script_generator",
  "steps": [
    {
      "id": "instructions",
      "action": "read_input",
      "file": "instructions.txt",
      "label": "قراءة التعليمات"
    },
    {
      "id": "topics",
      "action": "read_input",
      "file": "topics.json",
      "label": "قراءة العناوين"
    },
    {
      "id": "prompt",
      "action": "template",
      "text": "{instructions}\n\n---\n\nقائمة العناوين المطلوب إنشاء سكريبتات لها:\n\n{topics}"
    },
    {
      "id": "scripts",
      "action": "generate",
      "input": "{prompt}",
      "max_tokens": 16000,
      "temperature": 0.7,
      "label": "توليد السكريبتات"
    },
    {
      "id": "formatted",
      "action": "format_text",
      "input": "{scripts}",
      "color_words": ["بس", "ده", "دي", "دى", "دول"],
      "spaces_after_punctuation": 5,
      "save_as": "scripts_output.txt",
      "label": "تنسيق النص"
    }
  ]
}
```

### ملفات المستخدم المطلوبة في `input/`:
1. `instructions.txt` — تعليمات الكتابة للـ AI (الأسلوب، القواعد، التنسيق...)
2. `topics.json` — قائمة العناوين بالتنسيق: `[{"id": 1, "title": "..."}, ...]`

### النتيجة في `output/`:
- `scripts_output.txt` — السكريبتات المنسقة (فيها مسافات بعد الترقيم + كلمات ملونة)

---

## تعليمات لك كـ AI مساعد

عندما يطلب منك المستخدم إنشاء وصفة:

### 1. أرجع هذه الأشياء بالترتيب:

**أ. ملف الوصفة (JSON):**
- اسم الملف ومكانه: `recipes/اسم_الوصفة.json`
- محتوى الـ JSON كاملاً

**ب. ملفات الـ input المطلوبة:**
- اسم كل ملف ومحتواه (أو وصف ما يحطه المستخدم فيه)
- مكانها: `data/channels/اسم_القناة/اسم_الوصفة/input/`

**ج. تسجيل الوصفة:**
- ذكّر المستخدم يسجل الوصفة في الواجهة (http://localhost:8001)
- الاسم = نفس اسم الملف بدون `.json`
- الكود = `#`
- نوع المحتوى = `shorts` أو `long` أو `both` (حسب الغرض من الوصفة)

### 2. قواعد إلزامية:
- كل `id` لازم يكون فريد
- `{step_id}` لازم يشاور على خطوة سابقة فقط
- `save_as` في `tts` بدون امتداد (النظام بيضيف `.wav` و `.mp3`)
- `save_as` في باقي الأوامر بامتداد كامل (`.txt`, `.json`)
- ضيف خطوة `format_text` بعد `generate` **فقط لو المستخدم طلب تنسيق (مسافات أو تلوين)**. لو المستخدم ما ذكرش ده، متضفهاش
- لو المستخدم طلب تنسيق: لا تضع تعليمات التنسيق في تعليمات الـ AI — بايثون هيعمل ده عن طريق خطوة `format_text` في الوصفة
- `label` معامل اختياري لأي خطوة — وصف بالعربي يظهر في السجلات

### 3. توزيع المسؤوليات (بايثون vs AI):

**بايثون بيعمل (بدقة ومن غير أخطاء):**
- قراءة الملفات (`read_input`, `read_json`)
- تجميع النصوص (`template`)
- تنسيق النص: مسافات بعد الترقيم + تلوين كلمات (`format_text`)
- تحويل النص لصوت (`tts`)
- تحويل الصوت لنص (`transcribe`)
- حفظ الملفات (`save_file`)
- إرسال واستقبال الدفعات (`batch_send`, `batch_retrieve`)

**الـ AI بيعمل (حاجة واحدة بس):**
- كتابة/توليد النص (`generate`) — بناءً على التعليمات اللي في ملف `instructions.txt`

**قاعدة ذهبية:** أي حاجة ممكن تتعمل بدقة ثابتة (مسافات، تلوين، تحويل صيغ) = بايثون. أي حاجة محتاجة إبداع وفهم (كتابة نص) = AI.

**مهم: لا تضع تعليمات التنسيق (مسافات أو تلوين) في تعليمات الـ AI. بايثون هيعمل ده أوتوماتيك بعد استلام النص.**

### 4. ماذا يحتاج المستخدم أن يفعل يدوياً:

| الخطوة | مرة واحدة أم كل مرة؟ | المكان |
|--------|----------------------|--------|
| حفظ ملف الوصفة JSON | مرة واحدة | `recipes/اسم_الوصفة.json` |
| تسجيل الوصفة في الواجهة (الاسم + الكود `#` + النوع) | مرة واحدة | http://localhost:8001 |
| وضع ملفات الـ input (تعليمات + بيانات) | كل ما يحب يغيّر | `data/channels/القناة/الوصفة/input/` |
| الضغط على "شغّل" | كل مرة | الواجهة |
