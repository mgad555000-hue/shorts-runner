# تعليمات مشروع MG Ranner — Claude Code

أنت مساعد متخصص في نظام **MG Ranner** لبناء وتنصيب وتشغيل وصفات توليد محتوى فيديوهات.

---

## ⚡ كود `555000` — TRIGGER إجباري لتنفيذ الوصفة

**عندما يكتب المستخدم `555000` في نهاية رسالته، هذا أمر تنفيذ إجباري:**

1. **قف فوراً** — لا تكتب أي كود أو JSON من دماغك
2. **اقرأ هذا الملف** (CLAUDE.md) — قسم "بناء وصفة جديدة"
3. **اقرأ `RECIPE_GUIDE.md`** — المرجع الوحيد للأكشنز
4. **اقرأ القالب المناسب** من `templates/`
5. **نفّذ الخطوات بالترتيب** — نسخ القالب → تعديل الاسم → ملفات → DB → restart

**ممنوع تماماً:**
- ❌ بناء وصفة من الذاكرة أو من دماغك
- ❌ كتابة JSON بدون قراءة القالب أولاً
- ❌ اختراع أكشنز غير موجودة في RECIPE_GUIDE.md
- ❌ وضع تعليمات تنسيق في برومبت AI

**قواعد الـ trigger:**
- المستخدم ممكن يتكلم عن الوصفات ويناقش بحرية — **مفيش تنفيذ**
- التنفيذ الفعلي **لا يبدأ أبداً** إلا بعد ما يكتب `555000`
- `555000` = "ابدأ تنفيذ الوصفة من التعليمات الثابتة دلوقتي"

**مثال:**
```
المستخدم: عايز وصفة عامية لقناة My_Kidney اسمها نصائح_للكلى
← هنا نتناقش بس، مفيش تنفيذ

المستخدم: نفّذ الوصفة
555000
← هنا أبدأ التنفيذ من التعليمات والقوالب
```

---

## قواعد عامة

- **الرد بالعربي المصري دائماً**
- **ممنوع الاجتهاد في بناء الوصفات** — استخدم القوالب الثابتة من `templates/` أو اتبع `RECIPE_GUIDE.md` بالحرف
- **ممنوع اختراع أكشنز** غير موجودة في `RECIPE_GUIDE.md`
- **ممنوع وضع تعليمات تنسيق في برومبت AI** — التنسيق شغل بايثون (`format_text`)
- **اقرأ `RECIPE_GUIDE.md` قبل أي عمل وصفات** — هو المرجع الوحيد للأكشنز المتاحة

---

## بناء وصفة جديدة (خطوة بخطوة)

### المستوى 1: وصفة من نوع معروف (قالب جاهز)

لو المستخدم طلب وصفة من نوع موجود في `templates/`:

1. **اقرأ القالب** من `templates/recipe_النوع.json`
2. **انسخ القالب** إلى `recipes/اسم_الوصفة_الجديدة.json`
3. **غيّر `name`** في JSON لاسم الوصفة الجديدة
4. **غيّر نص `text` في خطوة `template`** لو الوصف مختلف (مثل: "سكريبتات" → "مقالات")
5. **اعمل مجلدات input + output**:
   - `data/channels/{القناة}/{اسم_الوصفة}/input/`
   - `data/channels/{القناة}/{اسم_الوصفة}/output/`
6. **انسخ `instructions.txt`** من:
   - لو المستخدم أعطاك تعليمات جديدة → استخدمها
   - لو لأ → انسخ من وصفة مشابهة موجودة
7. **انسخ `topics.json`** من وصفة موجودة في نفس القناة (أو القناة المشابهة)
8. **سجّل في DB** (شوف قسم "تسجيل في DB" تحت)
9. **docker-compose restart shorts-app** من مجلد `shorts-runner`

### المستوى 2: وصفة من نوع جديد (بناء من الدليل)

لو المستخدم طلب وصفة بتركيبة جديدة مش موجودة في القوالب:

1. **اقرأ `RECIPE_GUIDE.md` بالكامل**
2. **ركّب الخطوات من الـ 15 أكشن المتاحين فقط**:
   - `read_input`, `read_json`, `template`, `generate`, `format_text`, `save_docx`, `tts`, `tts_multi`, `transcribe`, `batch_send`, `batch_retrieve`, `save_file`, `read_docx`, `read_excel`, `copy_videos`
3. **اعرض الوصفة على المستخدم للموافقة قبل التنفيذ**
4. بعد الموافقة، نفّذ نفس خطوات المستوى 1 (مجلدات → ملفات → DB → restart)

### قواعد البناء الإلزامية:
- كل `id` لازم يكون فريد
- `{step_id}` لازم يشاور على خطوة **سابقة** فقط
- `format_text` تتضاف **بس لو** فيه تلوين أو مسافات إضافية
- `save_docx` آخر خطوة دايماً في وصفات توليد النصوص
- `save_as` في `tts` بدون امتداد
- `save_as` في باقي الأوامر بامتداد كامل
- `marker_prefix` = `"INTRO"` للمقدمات، default = `"SCRIPT"` للسكريبتات
- `temperature`: 0.3 للدقة (فصحى/تشكيل)، 0.7 للإبداع (عامية/ساخر)

### قاعدة تنسيق MG Ranner (إلزامية):
**كل نصوص المدخلات والمخرجات في أي وصفة لازم تكون بتنسيق MG Ranner.** هذه القاعدة تلقائية — مفيش نقاش عليها:

- **كل موضوع** محدد بماركرز: `<<<SCRIPT_N>>>...<<<END_SCRIPT>>>` (أو `<<<INTRO_N>>>` للمقدمات)
- **شورتس**: كل موضوع = قطعة نصية واحدة بين الماركرز
- **لونج**: كل موضوع فيه أجزاء: `<<<PART_1>>>...<<<END_PART>>>` لكل جزء
- **ينطبق على**: سكريبتات عامية، فصحى، ساخر، مقدمات، أو أي نوع تاني
- **العناوين (topics.json)**: كل عنوان له `id` — نفس الـ id بيظهر في الماركر
- **مفيش استثناء**: أي وصفة بتنتج أو بتستقبل نصوص = تنسيق MG Ranner

### قاعدة القنوات (إلزامية):
**كل وصفة لازم تكون channel-agnostic — نفس ملف الـ JSON يشتغل لأي قناة.** القناة بتتحدد وقت التشغيل من الواجهة:
- **ملف الوصفة JSON**: مفيهوش أي اسم قناة hardcoded — أسماء الملفات تكون عامة (مثل `videos_list.xlsx` مش `My_Kidney_videos_list.xlsx`)
- **مجلدات input**: كل قناة ليها مجلد input خاص فيه ملفاتها (instructions + بيانات)
- **الفيديوهات/القوائم**: كل قناة ليها فيديوهاتها وقوائمها — النظام بيختارهم أوتوماتيك حسب القناة المختارة
- **عند التنصيب**: لازم تعمل مجلدات input + output لكل القنوات الـ 3 وتحط ملفات كل قناة

### قاعدة الحد الأدنى من التغيير (إلزامية):
**أي تعليمات يعطيها المستخدم (instructions.txt أو أي نص تعليمات) لا تقم بإعادة صياغتها أو التغيير عليها إلا في أدنى الحدود بأقل التغييرات الواجب تغييرها لتناسب سياسة العمل فقط. التغييرات لا تكون جوهرية نهائياً بل تغييرات بسيطة جداً وبشكل يضمن عدم تغيير المقصود من النص حتى ولو بمعنى مماثل.**

### المراجعة النهائية الإلزامية (قبل إعلان جاهزية الوصفة):
**بعد بناء أي وصفة وقبل ما تقول "الوصفة جاهزة"، لازم تنفّذ الـ checklist ده بالكامل وتعرض النتيجة:**

1. **ملف الوصفة JSON**: موجود في `recipes/` — صالح (valid JSON) — كل الخطوات بترجع لخطوات سابقة صح
2. **تسجيل DB**: الوصفة موجودة في `shorts_recipes` — `input_folder` مطابق لاسم المجلد الفعلي — `created_at` و `updated_at` مش NULL
3. **مجلدات Input (كل القنوات الـ 3)**: المجلد موجود لكل قناة — الملفات المطلوبة موجودة (instructions.txt + أي ملفات تانية)
4. **مجلدات Output (كل القنوات الـ 3)**: موجودة وفاضية
5. **API الوصفات**: `GET /api/utilities/recipes` بيرجع الوصفة الجديدة
6. **API المسارات**: `GET /api/channels/{channel}/recipe-path/{recipe_name}` بيرجع المسار الصح لكل قناة
7. **API القنوات**: `GET /api/channels` بيرجع القنوات الـ 3
8. **Docker**: الـ container شغال بعد restart — الـ API بيرد
9. **تطابق الأسماء**: `sanitize_folder_name(recipe.name).replace(' ', '_')` == اسم المجلد الفعلي على الـ disk
10. **الأكشنز**: كل أكشن مستخدم في الوصفة موجود ومسجّل في `ACTIONS` dict في `recipe_runner.py`

**ممنوع تقول "الوصفة جاهزة" إلا لما الـ 10 بنود كلهم PASS. لو أي بند فشل → أصلحه الأول.**

---

## القوالب المتاحة

| القالب | الملف | الوصف |
|--------|-------|-------|
| عامية | `templates/recipe_عامية.json` | generate(0.7) → format_text → save_docx |
| فصحى | `templates/recipe_فصحى.json` | generate(system_prompt, 0.3) → save_docx (بدون format_text) |
| مقدمات | `templates/recipe_مقدمات.json` | generate(0.3) → format_text → save_docx(marker_prefix=INTRO) |
| ساخر | `templates/recipe_ساخر.json` | generate(0.7) → format_text → save_docx |

---

## تسجيل في DB

**استخدم Python sqlite3 مباشرة — ممنوع curl (بيبوظ العربي في Windows)**

```python
python3 -c "
import sqlite3
from datetime import datetime
now = datetime.now().isoformat()
conn = sqlite3.connect('data/database.db')
conn.execute('''INSERT INTO shorts_recipes (name, description, code, input_folder, created_at, updated_at, recipe_type)
VALUES (?, ?, ?, ?, ?, ?, ?)''',
('اسم الوصفة', 'وصف مختصر', '#', 'اسم_مجلد_الوصفة', now, now, 'shorts'))
conn.commit()
conn.close()
print('Recipe registered successfully')
"
```

**ملاحظات DB — schema الجدول الفعلي:**
- `name` = اسم الوصفة بالعربي (بمسافات)
- `description` = وصف مختصر (اختياري)
- `code` = دايماً `#`
- `input_folder` = اسم مجلد الوصفة (بدون مسار، بأندرسكور — زي `إنشاء_سكريبت_بالعامية`)
- `recipe_type` = `shorts` أو `long` (الافتراضي: `shorts`)
- **لازم `created_at` و `updated_at`** — لو NULL الـ API بيقع (Pydantic validation error)
- **القناة مش في DB** — بتتحدد وقت التشغيل من الواجهة

---

## بنية المجلدات

```
shorts-runner/
├── recipes/                          ← ملفات الوصفات JSON
├── templates/                        ← القوالب الثابتة (لا تُعدَّل)
├── data/
│   ├── database.db                   ← قاعدة البيانات
│   └── channels/
│       └── {القناة}/
│           └── {اسم_الوصفة}/
│               ├── input/            ← instructions.txt + topics.json
│               └── output/           ← المخرجات
├── RECIPE_GUIDE.md                   ← دليل الأكشنز (المرجع الوحيد)
└── COWORK_PROJECT.md                 ← مستند Cowork
```

---

## القنوات

| القناة | النوع |
|--------|-------|
| My_Kidney | طبية — كلى |
| Alhashab2000 | — |
| Social_relations | علاقات اجتماعية |

---

## الفرق بين شغل بايثون وشغل AI

| بايثون (دقيق وثابت) | AI (إبداعي) |
|---------------------|-------------|
| read_input, read_json | generate |
| template | — |
| format_text (تلوين + مسافات) | — |
| save_docx, save_file | — |
| tts, transcribe | — |
| batch_send, batch_retrieve | — |

**القاعدة الذهبية:** أي حاجة تتعمل بدقة ثابتة = بايثون. أي حاجة محتاجة إبداع = AI.

---

## Docker

- **أي تعديل Python** → محتاج `docker-compose restart shorts-app`
- **تعديل JSON أو input files** → مش محتاج restart
- الأمر يتنفذ من مجلد: `C:\Users\w10\shorts-runner`

---

## الاختصارات

| يكتب | أعمل |
|------|------|
| `1` | أشوف الملفات في `C:\Users\w10\Downloads\1` |
| `9` | أقرأ `responses.md` وأرد فيه (overwrite) |
| `0` | أبعت آخر رد على المتصفح |
| `تحقق` | أشوف آخر تشغيل للوصفة |
| `نصّب` | أجيب الملفات من `Downloads/recipes_exchange` وأنصّب |
| `شغّل` | أشغّل آخر وصفة من الـ API |
| `لوج` | أعرض آخر لوجات Docker |
| `ريستارت` | `docker-compose restart shorts-app` |
| `راجع` | أقرأ آخر ملف Word مخرج |
| `امسح` | أمسح output قديم |
| `وصفات` | أعرض قائمة الوصفات من الـ API |
| `فحص` | أفحص آخر `texts_output.docx` بسكريبت `tools/check_texts_docx.py` |
| `فحص N` | أفحص ملف `texts_output.docx` في `OneDrive\Desktop\New folder\N` |
| `فحص <path>` | أفحص الملف في المسار المحدد |
| `فحص json` | فحص + حفظ JSON تفصيلي + تحليل عميق |
| **`555000`** | **⚡ TRIGGER تنفيذ وصفة — أقرأ CLAUDE.md + RECIPE_GUIDE + القالب وأنفّذ بالخطوات** |
