# التقرير الكامل من اكتشاف فجوة المرحلة الدلالية حتى إقفالها

تاريخ التقرير: 18 يوليو 2026
نقطة البداية: رسالة المستخدم التي حددت أن المرحلة الدلالية لم تختبر على موديل
حقيقي، وأن ملفات التنفيذ السبعة لم تكن مثبتة في Git
نقطة النهاية: نجاح كناري Vertex النظيف والمزروع بعد إصلاح ما كشفه الموديل
الحقيقي، وتثبيت الكود والاختبارات والتوثيق في Git

## 1. الطلب الذي بدأ منه العمل

الرسالة حددت فجوتين واضحتين:

1. المرحلة الدلالية الثقيلة لم تكن قد مرت على موديل حقيقي:
   - 22 حكما بأدلة لكل موضوع.
   - 6 مقارنات داخل كل موضوع.
   - طلب عالمي يرى الموضوعات معا.
   - بصمة سياسة وهوية طلب.
2. ملفات التنفيذ السبعة كانت موجودة في شجرة العمل ولم تكن مكومتة في Git.

المطلوب العملي أصبح:

- تثبيت الكود في Git من غير خلطه بتغييرات المستخدم الأخرى.
- تجهيز حالتي كناري سليمتين بنيويا.
- تشغيل Vertex Batch حقيقي، وليس Parser أو محاكي فقط.
- إثبات أن الحالة النظيفة لا تنتج إنذارات كاذبة.
- زرع عيب دلالي حقيقي وإثبات اكتشافه.
- إصلاح أي فرق يكشفه الموديل الحقيقي.
- إعادة التشغيل من صورة مبنية من Git نظيف.
- حفظ الأدلة والتكلفة والنتائج.

## 2. مراجعة حالة Git قبل أي تشغيل حقيقي

تم فحص شجرة العمل، وكانت متسخة بتغييرات وملفات كثيرة لا تخص مراجع تكست
لونج. لذلك لم يتم استخدام إضافة جماعية للملفات.

تم عزل ملفات الإنتاج السبعة فقط:

1. `app/long_text_contract.py`
2. `app/long_text_contract.v1.json`
3. `app/long_text_reviewer.py`
4. `app/review_evidence.py`
5. `app/long_text_semantic_reviewer.py`
6. `app/long_text_review_runtime_final.py`
7. `recipes/مراجعة_مخرجات_تكست_لونج.py`

تم التحقق من عدم إدخال:

- تعديلات `app/database.py` و`app/engine.py` و`app/main.py` وغيرها.
- ملفات Runtime التاريخية غير المستخدمة في الإنتاج.
- مجلدات الاختبار المؤقتة.
- التقارير والملفات الشخصية الموجودة مسبقا.
- ملفات الخطوط أو ملفات الوصفات الأخرى المعدلة.

## 3. أول كوميت: تثبيت ملفات الإنتاج السبعة

الكوميت:

`c6ae4ee إضافة مراجع مخرجات تكست لونج الإنتاجي`

وقت الكوميت:

18 يوليو 2026، الساعة 00:19 بتوقيت القاهرة.

أضاف الكوميت الملفات السبعة فقط. أهم ما ثبته:

- العقد المركزي وبصمته.
- البوابة البنيوية.
- Runtime الإنتاج الموحد.
- إنشاء 22 حكما و6 مقارنات لكل موضوع.
- إنشاء طلب عالمي عند وجود موضوعين أو أكثر.
- بصمة السياسة وهوية المحتوى والدفعة.
- التحقق من الأدلة.
- الفشل المغلق عند أي رد ناقص أو غير موثوق.
- وصفة إم جي رانر ذات الخطوات السبع.

من هذه اللحظة اتقفلت فجوة أن كود الإنتاج نفسه غير مكومت.

## 4. مراجعة اعتماد الساندبوكس والاختبارات

بعد تثبيت الملفات السبعة، تم فحص هل الكوميت وحده كاف لبناء صورة نظيفة.

ظهر نقص مهم:

- `sandbox.Dockerfile` لم يكن ينسخ مجلد `app` إلى `/app/app`.

لو تم بناء الصورة من Git نظيف في هذه الحالة، قد تنجح أجزاء من البناء لكن كود
الوصفات الجديد لن يكون موجودا داخل الصورة كما هو مطلوب.

تم تعديل:

- `sandbox.Dockerfile` لإضافة `COPY app /app/app`.
- `.dockerignore` لاستبعاد:
  - `app/tests/`
  - مجلدات `app/_*/` المؤقتة.

كما تم تثبيت 6 ملفات اختبارات:

1. `app/tests/test_long_text_reviewer.py`
2. `app/tests/test_long_text_contract_runtime.py`
3. `app/tests/test_long_text_semantic_global.py`
4. `app/tests/test_long_text_policy_guards.py`
5. `app/tests/test_long_text_pipeline_context.py`
6. `app/tests/test_review_evidence.py`

## 5. ثاني كوميت: الساندبوكس والاختبارات

الكوميت:

`49e3de1 تثبيت اعتماد الساندبوكس واختبارات مراجع تكست لونج`

وقت الكوميت:

18 يوليو 2026، الساعة 00:34 بتوقيت القاهرة.

بعده تم:

- بناء صورة من `git archive` للكوميت نفسه، وليس من شجرة العمل المتسخة.
- اختبار استيراد الوصفة داخل الصورة.
- اختبار الاستيراد مع الشبكة مغلقة.

نجح الاستيراد. لكن هذا ظل إثباتا للكود والبيئة فقط، وليس إثباتا لموديل حقيقي.

## 6. تجهيز الكناري الحقيقي

تم إنشاء جذر أدلة مستقل:

`C:\tmp\long_text_semantic_canary_49e3de1_v3`

تم تجهيز موضوعين:

| المعرف | العنوان |
|---:|---|
| 2501 | علامات مبكرة تستحق الانتباه لصحة الكلى |
| 2502 | كيف يؤثر الملح الزائد على صحة الكلى |

استخدام موضوعين كان مقصودا لأنه أقل حجم يختبر في نفس الوقت:

- مراجعة مستقلة للموضوع 2501.
- مراجعة مستقلة للموضوع 2502.
- بوابة عالمية واحدة تقارن الموضوعين.

كل حالة احتوت:

- `topics.json`
- `scripts_output.docx`
- `thumbnail_texts.docx`
- `instructions.txt`
- `review_contract.json`

## 7. الكناري النظيف

الموضوعان في المجال الطبي نفسه، لكن كل واحد يتناول فكرة مختلفة فعلا.

الغرض:

- قياس الإنذارات الكاذبة.
- إثبات أن تشابه مجال الكلى وحده لا يعتبر نسخا.
- إثبات مرور 22 حكما و6 مقارنات والطلب العالمي من غير مخالفات.

النتيجة المتوقعة:

- `review_completed=true`
- `all_clear=true`
- لا أخطاء دلالية.
- لا أخطاء استجابة.
- لا موضوعات لإعادة التوليد.

## 8. الكناري المزروع

تم نسخ الحالة النظيفة، ثم تعديل بند واحد فقط:

`translation_fr_description` في الموضوع 2502.

بدلا من ترجمة وصف تأثير الملح على الكلى، تم وضع ترجمة فرنسية تتحدث عن
العلامات المبكرة التي تستحق تقييم الكلى. الصياغة قريبة من ترجمة الموضوع 2501،
لكنها ليست نسخة حرفية كاملة.

هذا العيب يختبر شيئين:

1. هل المراجعة الفردية تكتشف ترجمة غير أمينة؟
2. هل البوابة العالمية تكتشف النقل المعاد صياغته بين موضوعين؟

النتيجة المتوقعة:

- `BAD_TRANSLATION` في الموضوع 2502.
- فشل `TRANSLATIONS_FAITHFUL`.
- علاقة عالمية `COPIED`.
- كود `DUPLICATION`.
- إعادة الموضوعين 2501 و2502.

## 9. فحص ما قبل الإرسال

تم تمرير الحالتين على البوابة الحتمية قبل Vertex.

| الحالة | أخطاء البنية | التحذيرات | الموضوعات | الطلبات |
|---|---:|---:|---:|---:|
| النظيف | 0 | 0 | 2 | 3 |
| المزروع | 0 | 0 | 2 | 3 |

بصمة السياسة الفعالة:

`75ccea121d4967b3e97ba1f34aed07456babf364f443fa16a07bc241c972c18b`

بصمة العقد:

`sha256:dbb09a76ea2d05f72c47ffb97a53346ca2247b5456911908d5a38c72240bf255`

كل طلب موضوع احتوى:

- 22 عنصرا في `field_reviews`.
- 6 عناصر في `cross_checks`.
- `topic_id`.
- `request_id`.
- `policy_sha256`.

والطلب العالمي احتوى بيانات الموضوعين وبصمة الدفعة.

## 10. محاولة التشغيل من واجهة إم جي رانر

تمت محاولة استخدام المسار المحلي لواجهة إم جي رانر، لكن جسر المتصفح اصطدم
بخطأ Windows Sandbox:

`SetTokenInformation(TokenDefaultDacl) failed: 1344`

لم يتم اعتبار ذلك فشلا في وصفة المراجعة أو Vertex؛ هو فشل في جسر التحكم
المحلي. بدلا من إيقاف المهمة، تم استخدام نفس مسار الإنتاج داخل Docker
والإرسال المباشر عبر Vertex Batch.

## 11. إعدادات التشغيل الحقيقي

الموديل:

`gemini-2.5-flash`

الطريقة:

Vertex AI Batch Prediction.

الموقع:

`us-central1`

عدد الطلبات لكل تشغيل:

3.

إعدادات الوصفة:

- `temperature=0.05`
- `max_tokens=24000`
- `fail_incomplete=true`

لم يتم استخدام `GEMINI_API_KEY`.

تم استخدام بيانات Vertex OAuth المصرح بها فقط. لم تتم طباعة الأسرار أو
حفظها داخل التقرير.

## 12. الجولة الحقيقية الأولى

معرفا التشغيل:

- النظيف: `lt-canary-clean-20260718-0045`
- المزروع: `lt-canary-seeded-20260718-0051`

Vertex أعاد:

- 3 ردود كاملة للنظيف.
- 3 ردود كاملة للمزروع.
- لا يوجد truncation.
- كل رد موضوع احتوى 22 حكما و6 مقارنات.
- الرد العالمي موجود في الحالتين.

لكن التشغيلين فشلا مغلقا في الـParser.

## 13. المشكلة الأولى التي كشفها Vertex

Gemini غلف كل الردود الستة داخل غلاف كود `json` رغم أن:

- Prompt طلب كائن JSON خاما فقط.
- System prompt طلب كائنا واحدا.
- التعليمات منعت أسوار الكود صراحة.

الـParser القديم كان يقبل كائن JSON خاما فقط، ولذلك رفض الردود.

نتيجة التقرير الرسمي في الجولة الأولى:

| الحالة | `review_completed` | الموضوعات المحكوم عليها | أخطاء الاستجابة | البوابة العالمية |
|---|---|---:|---:|---|
| النظيف | `false` | 0 من 2 | 6 | غير مكتملة |
| المزروع | `false` | 0 من 2 | 6 | غير مكتملة |

تم إدخال الموضوعين إلى قائمة إعادة التوليد لأن الردود غير موثوقة، ولم يمر
النظام بفشل صامت.

## 14. المشكلة الثانية التي كشفها Vertex

بعد إزالة غلاف واحد محليا للتشخيص فقط، مع الحفاظ على الملفات الخام، ظهرت
مشكلة الأدلة:

- دليل كلمات مفتاحية بطول 54 كلمة في الكناري النظيف.
- دليل كلمات مفتاحية بطول 72 كلمة في الكناري النظيف.
- دليل عالمي بطول 14 كلمة في الكناري المزروع.

العقد يسمح من 3 إلى 12 كلمة فقط.

المحاكي لم يكشف هذه الحالة لأن الردود الاصطناعية كانت تلتزم بالطول المطلوب.

## 15. ما أثبته التشخيص قبل الإصلاح

بعد إزالة الغلاف لأغراض التشخيص:

- الموديل حكم الكناري النظيف سليما.
- اكتشف خطأ الترجمة في الموضوع 2502.
- فشل `TRANSLATIONS_FAITHFUL`.
- اكتشف النقل المعاد صياغته بين 2501 و2502.
- استخدم العلاقة `COPIED`.
- استخدم الكود `DUPLICATION`.

إذن الموديل فهم العيب المقصود. الفشل كان في توافق بروتوكول الرد وطول الدليل،
وليس في القدرة الدلالية نفسها.

## 16. قرار الإصلاح

لم يتم فتح الـParser لقبول أي نص حول JSON، ولم يتم تعطيل شرط الدليل.

تم اختيار إصلاح محدود مبني فقط على السلوك الحقيقي المثبت:

1. قبول غلاف JSON واحد مضبوط.
2. اختصار دليل طويل فقط عندما يكون الدليل الكامل اقتباسا حرفيا متصلا.
3. استمرار رفض أي رد أو دليل لا يثبت محليا.
4. الاحتفاظ بالرد الخام من غير تعديل.

## 17. إصلاح غلاف JSON

أصبح الـParser يقبل غلافا واحدا فقط إذا:

- أول سطر علامة غلاف عادية أو علامة `json`.
- آخر سطر علامة الإغلاق.
- لا يوجد نص قبل الغلاف أو بعده.
- لا توجد أسوار أخرى داخله.
- المحتوى الداخلي كائن JSON واحد.
- لا توجد مفاتيح مكررة.

ويستمر في رفض:

- شرح قبل JSON أو بعده.
- غلاف ناقص.
- أكثر من غلاف.
- غلاف متداخل.
- JSON تالف.
- أكثر من كائن.
- مفاتيح مكررة.

## 18. إصلاح الدليل الطويل

لا يتم اختصار الدليل إلا إذا:

1. الدليل الكامل موجود حرفيا ومتصل داخل مصدر واحد.
2. الزيادة الوحيدة هي تجاوز 12 كلمة.
3. يمكن أخذ بادئة أصلية من المصدر بين 3 و12 كلمة.
4. البادئة تجتاز نفس محقق الأدلة.

لا يتم:

- اختراع دليل.
- إعادة صياغة دليل.
- تجميع كلمات متفرقة.
- استخدام دليل من حقل آخر.
- إصلاح دليل غير موجود بالكامل في المصدر.
- تغيير الرد الخام المحفوظ.

## 19. تقوية Prompt

تمت إضافة تعليمات أكثر صراحة:

- لا تنسخ قائمة الكلمات المفتاحية أو الهاشتاجات كاملة في `evidence`.
- اختر من 3 إلى 12 كلمة متصلة.
- ابدأ الرد بحرف `{`.
- أنه الرد بحرف `}`.
- لا تستخدم غلاف كود.

هذه التعليمات حسنت التحديد، لكنها لم تكن بديلا عن إصلاح الـParser؛ الجولة
الثانية أثبتت أن Gemini استمر في استخدام الغلاف.

## 20. اختبارات الإصلاح

تمت إضافة 4 اختبارات مرتبطة مباشرة بالفشل الحقيقي:

1. قبول غلاف JSON واحد.
2. رفض النص الخارجي والأسوار المتعددة أو الناقصة.
3. اختصار اقتباس طويل حرفي إلى بادئة مثبتة.
4. رفض إصلاح اقتباس طويل غير حرفي، مع عدم تغيير اقتباس صالح.

مجموعة الإنتاج المتتبعة أصبحت:

| ملف الاختبار | العدد |
|---|---:|
| `test_long_text_reviewer.py` | 9 |
| `test_long_text_contract_runtime.py` | 16 |
| `test_long_text_semantic_global.py` | 8 |
| `test_long_text_policy_guards.py` | 3 |
| `test_long_text_pipeline_context.py` | 1 |
| `test_review_evidence.py` | 8 |
| الإجمالي | 45 |

النتيجة:

`45 من 45 ناجحة`

تشغيل اكتشاف أوسع نفذ أيضا 4 اختبارات Runtime قديمة غير متتبعة في Git،
فنجحت 49 من 49 محليا. الرقم الرسمي لمجموعة الإنتاج هو 45.

## 21. ثالث كوميت: إصلاح Vertex الحقيقي

الكوميت:

`078551a إصلاح توافق ردود Vertex الحقيقية لمراجع تكست لونج`

وقت الكوميت:

18 يوليو 2026، الساعة 01:02 بتوقيت القاهرة.

الملفات المعدلة:

- `app/long_text_semantic_reviewer.py`
- `app/review_evidence.py`
- `app/tests/test_long_text_semantic_global.py`
- `app/tests/test_review_evidence.py`

لم يدخل الكوميت أي ملف غير مرتبط بالإصلاح.

## 22. إعادة تحليل الردود الخام القديمة

قبل دفع تكلفة جولة ثانية، تم تمرير نفس ردود الجولة الأولى الخام عبر الكود
بعد الإصلاح.

النتيجة:

- النظيف:
  - `review_completed=true`
  - `all_clear=true`
  - 0 مخالفات دلالية.
  - 0 أخطاء استجابة.
- المزروع:
  - `review_completed=true`
  - `all_clear=false`
  - 4 مخالفات دلالية.
  - 0 أخطاء استجابة.
  - إعادة 2501 و2502.

هذا أثبت أن الإصلاح يعالج الرد الحقيقي المحفوظ، لكنه لم يكن بديلا عن إعادة
تشغيل Vertex من البداية.

## 23. بناء الصورة النهائية من Git نظيف

تم بناء صورة جديدة من `git archive` للكوميت:

`078551a`

اسم الصورة المرجعية:

`shorts-runner-sandbox:git-078551a`

هوية الصورة:

`sha256:e942b96bc36951d10d04f69719f663cca6afebac2bb2b2ca8c084775333ffc0d`

تم:

- اختبار الاستيراد مع الشبكة مغلقة.
- التأكد أن الصورة مبنية من محتوى Git.
- ترقية الصورة نفسها إلى:

`shorts-runner-sandbox:latest`

الوسمان يشيران إلى هوية الصورة نفسها.

## 24. الجولة الحقيقية الثانية

معرفا التشغيل:

- النظيف: `lt-canary-clean-r2-20260718-0103`
- المزروع: `lt-canary-seeded-r2-20260718-0103`

كل تشغيل أرسل 3 طلبات واستلم 3 ردود.

الردود الستة في الجولة الثانية استمرت في استخدام غلاف `json`. مرورها بنجاح
يثبت أن إصلاح الـParser كان ضروريا، وأن تقوية Prompt وحدها لم تكن كافية.

## 25. نتيجة الكناري النظيف النهائية

- Exit code: 0
- `review_completed=true`
- `all_clear=true`
- `cross_topic_completed=true`
- `topics_judged=2`
- `structure_error_count=0`
- `semantic_error_count=0`
- `response_error_count=0`
- `regenerate_topics=[]`

استهلاك التوكنز:

- Input: 13051
- Output: 3944
- Thinking: 0
- Total: 16995

الحكم:

النظام لم يطلق إنذارا كاذبا بسبب تشابه مجال الكلى، واعتبر الموضوعين سليمين.

## 26. نتيجة الكناري المزروع النهائية

- Exit code: 0
- `review_completed=true`
- `all_clear=false`
- `cross_topic_completed=true`
- `topics_judged=2`
- `structure_error_count=0`
- `semantic_error_count=4`
- `response_error_count=0`
- `regenerate_topics=[2501, 2502]`

المخالفات الأربع:

| الموضوع | البند أو العلاقة | الكود |
|---:|---|---|
| 2502 | `translation_fr_description` | `BAD_TRANSLATION` |
| 2502 | `TRANSLATIONS_FAITHFUL` | `BAD_TRANSLATION` |
| 2501 | `CROSS_TOPIC_COPIED_WITH_2502` | `DUPLICATION` |
| 2502 | `CROSS_TOPIC_COPIED_WITH_2501` | `DUPLICATION` |

استهلاك التوكنز:

- Input: 13061
- Output: 4253
- Thinking: 0
- Total: 17314

الحكم:

المراجعة الفردية اكتشفت الترجمة الخاطئة، والبوابة العالمية اكتشفت النقل
المعاد صياغته، وأعيد الطرفان للتوليد.

## 27. إجمالي التشغيل الحقيقي

تم تنفيذ:

- 4 مهام Vertex Batch.
- 12 استجابة موديل.
- 8 ردود لمراجعة الموضوعات.
- 4 ردود للبوابة العالمية.

إجمالي التوكنز:

- Input: 51836
- Output: 16638
- Thinking: 0
- Total: 68474

التكلفة الحسابية:

`0.0285729 دولار`

التكلفة بالتقريب:

`حوالي 0.029 دولار`

ملفا `usage_summary.json` يثبتان أرقام الجولة الثانية. مجلدا الجولة الأولى لا
يحتويان ملف استخدام مستقلا؛ أرقامهما والإجمالي مأخوذة من سجل التنفيذ والتقرير
المحفوظ.

## 28. حفظ الأسرار وتنظيف الحاويات

- لم يستخدم `GEMINI_API_KEY`.
- استخدم مسار Vertex OAuth المصرح به.
- لم تتم طباعة قيم OAuth في التقارير.
- تمت إزالة حاويات الجولات بعد حفظ النتائج.

سبب إزالة الحاويات:

منع بقاء بيانات OAuth داخل حاويات متوقفة يمكن فحص إعداداتها لاحقا.

## 29. رابع كوميت: توثيق الجولة الحقيقية

الكوميت:

`96f2a0c توثيق اختبار Vertex الحقيقي لمراجع تكست لونج`

وقت الكوميت:

18 يوليو 2026، الساعة 01:15 بتوقيت القاهرة.

أضاف:

`LONG_TEXT_REVIEW_REAL_VERTEX_CANARY_REPORT_2026-07-18.md`

هذا الكوميت توثيق فقط، ولم يغير كود الإنتاج.

## 30. التسلسل الزمني المختصر

| الوقت بتوقيت القاهرة | ما تم |
|---|---|
| 00:19 | تثبيت ملفات الإنتاج السبعة في `c6ae4ee` |
| 00:34 | تثبيت الساندبوكس والاختبارات في `49e3de1` |
| 00:45 | إرسال الكناري النظيف في الجولة الأولى |
| 00:51 | إرسال الكناري المزروع في الجولة الأولى |
| بعد الجولة الأولى | تشخيص غلاف JSON والأدلة الطويلة |
| 01:02 | تثبيت الإصلاح في `078551a` |
| 01:03 | إرسال الكناريين في الجولة الثانية من الصورة النظيفة |
| بعد اكتمال الجولة | نجاح النظيف واكتشاف عيوب المزروع |
| 01:15 | تثبيت تقرير Vertex في `96f2a0c` |

## 31. حالة Git عند نهاية العمل

الكوميتات الخاصة بالمهمة:

1. `c6ae4ee`
2. `49e3de1`
3. `078551a`
4. `96f2a0c`

قبل إضافة هذا التقرير:

- `HEAD=96f2a0c`
- الفرع `master`.
- الفرع المحلي سابق `origin/master` بأربع كوميتات.
- الكود مثبت محليا.
- الكوميتات لم تدفع إلى الريموت.

تغييرات المستخدم الأخرى ما زالت موجودة كما كانت، ولم تدخل هذه الكوميتات.

## 32. أماكن الأدلة

الجذر:

`C:\tmp\long_text_semantic_canary_49e3de1_v3`

فحص ما قبل الإرسال:

`C:\tmp\long_text_semantic_canary_49e3de1_v3\preflight_summary.json`

الجولة الأولى النظيفة:

`C:\tmp\long_text_semantic_canary_49e3de1_v3\lt-canary-clean-20260718-0045`

الجولة الأولى المزروعة:

`C:\tmp\long_text_semantic_canary_49e3de1_v3\lt-canary-seeded-20260718-0051`

الجولة الثانية النظيفة:

`C:\tmp\long_text_semantic_canary_49e3de1_v3\lt-canary-clean-r2-20260718-0103`

الجولة الثانية المزروعة:

`C:\tmp\long_text_semantic_canary_49e3de1_v3\lt-canary-seeded-r2-20260718-0103`

داخل كل مجلد:

- `batch_job_info.json`
- `long_text_review_requests.json`
- `long_text_review_responses.json`
- `long_text_structure_report.json`
- `long_text_review_report.json`
- `long_text_review_report.txt`
- `long_text_topics_to_regenerate.json`
- لقطة الموضوعات المستخدمة.

## 33. ملاحظات تدقيق الأدلة

1. `batch_job_info.json` يحفظ حالة `submitted` لأنه لقطة وقت الإرسال. إثبات
   الإتمام هو وجود الردود الثلاثة والتقرير النهائي.
2. Exit code 0 موثق من جلسة التشغيل، ولا يوجد ملف `exit-status` مستقل.
3. ملفات الجولة الأولى لا تحتوي `usage_summary.json` مستقلا.
4. كل الردود الـ12 الخام ما زالت موجودة داخل أغلفة `json`.
5. الرد الخام لم يتغير عند اختصار الدليل في التقرير المتحقق منه.
6. صورة `latest` وصورة `git-078551a` لهما الهوية نفسها.

## 34. ما اتقفل وما لم يتقفل

### اتقفل

- السبعة ملفات لم تعد غير مكومتة.
- اختبارات الإنتاج مثبتة في Git.
- الساندبوكس أصبح يحتوي كود `app`.
- المرحلة الدلالية اتجربت على Vertex حقيقي.
- الكناري النظيف مر بلا false positive.
- الكناري المزروع كشف الترجمة والنقل.
- فروق غلاف JSON والدليل الطويل اتصلحت.
- الإصلاح اتجرب مرة ثانية على Vertex.
- الصورة النهائية مبنية من Git نظيف.

### لم يتقفل

- الكوميتات لم تدفع إلى الريموت.
- أدلة الكناري موجودة في `C:\tmp` وليست في مخزن دائم.
- الاختبار الحقيقي اقتصر على موضوعين ونوع عيب مزروع واحد مركب.
- لم يتم تشغيل كناري حقيقي لكل كود من الأكواد الـ17 أو لكل علاقة عالمية.
- الحكم الدلالي يظل احتماليا، رغم حمايات الهوية والأدلة والفشل المغلق.

## 35. القرار النهائي

الفجوة التي حددتها الرسالة الأصلية اتقفلت:

- الكود لم يعد سبعة ملفات سائبة؛ أصبح مثبتا في Git مع الساندبوكس والاختبارات.
- المرحلة الدلالية لم تعد معتمدة على المحاكي فقط.
- Vertex الحقيقي كشف مشكلتين لم ترهما المحاكاة.
- المشكلتان اتصلحتا بقيود آمنة.
- إعادة التشغيل من Git نظيف نجحت.
- الحالة النظيفة مرت بلا إنذار كاذب.
- الحالة المزروعة أنتجت المخالفات المطلوبة وأعادت الطرفين للتوليد.

المتبقي ليس فجوة تمنع إثبات العمل الحالي، لكنه عمل تشغيلي لاحق:

1. دفع الكوميتات إلى الريموت.
2. نقل أدلة الكناري من `C:\tmp` إلى مكان دائم.
3. توسيع بنك الكناري مستقبلا ليغطي دفعات أكبر وعلاقات وأكواد إضافية.
